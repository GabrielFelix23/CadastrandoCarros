<h1>Registros de postagens: </h1>
{{#each cadastros}}
    <div class="container">
        <div class="mt-4">
            <h2>Vendedor: {{nomeUser}}</h2>
            <h3>cadastro: <a href="/cadastro/postagem/{{nomeCarro}}">{{nomeCarro}}</a></h3>
        </div>
    </div>
{{else}}
{{/each}}