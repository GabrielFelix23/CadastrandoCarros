{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}
{{/each}}

<h3>Postagem</h3>

<div class="card">
    <div class="card-body">
        <form action="">
            <label for="titulo">Titulo</label>
            <input type="text" id="titulo" name="titulo" class="form form-control">

            <label for="nome">Nome do carro</label>
            <input type="text" id="nome" name="nome" class="form form-control">

            <label for="ano">Ano do carro</label>
            <input type="text" id="ano" name="ano" class="form form-control">

            <label for="km">Quantos Km/h</label>
            <input type="text" id="km" name="km" class="form form-control">

            <label for="preco">Preço do carro</label>
            <input type="text" id="preco" name="preco" class="form form-control">

            <label for="postagem">Postagem</label>
            <select name="postagem" class="custom-select">
                {{#each postagens}}
                    <option value="{{_id}}">{{nome}}</option>
                {{else}}
                    <option value="0">Nenhum valor inserido</option>
                {{/each}}
            </select>

            <button type="submit" class="btn btn-success mt-4">Postar</button>
            <a href="/carros">
                <button type="button" class="btn btn-primary mt-4">Voltar</button>
            </a>
        </form>
    </div>
</div>