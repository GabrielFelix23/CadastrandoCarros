<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
          <a class="navbar-brand" href="/">
            <h1>Tec<sapan style="color: red;">Car</sapan></h1>
          </a>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
      
          <div class="collapse navbar-collapse d-lg-flex justify-content-end" id="navbarSupportedContent">
            <ul class="navbar-nav">
              <li class="nav-item active">
                <a class="nav-link" href="/">Home</a>
              </li>
              <li class="nav-item active">
                <a class="nav-link" href="/cadastros/lista">Cadastros</a>
              </li>

              {{#if user}}
              <div class="dropdown">
                <button class="btn dropdown-toggle" style="color: white;" type="button" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                  Configurações
                </button>
                <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                  <a class="dropdown-item" href="/usuarios/registro/usuario">Editar perfil</a>
                  <a class="dropdown-item" style="color: red;" href="/logout">Sair</a>
                </div>
              </div>
                <h5 style="color: white; margin-top: 7px">| &nbsp; Olá,&nbsp;</h5>
                {{#each user}}
                  <h5 style="color: red; margin-top: 7px"> {{nome}}</h5>
                {{else}}
                {{/each}} 

              {{else}}
              <li class="nav-item active">
                <a class="nav-link" href="/usuarios/login" style="color: red;">Login</a>
              </li>
              {{/if}}
            </ul>    
                 
          </div>
        </div>
</nav>  